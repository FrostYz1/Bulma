import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

import Switch from '../Switch';
describe('Switch', function () {
  test('renders by default', function () {
    var checkbox = create(renderWithTheme( /*#__PURE__*/React.createElement(Switch, {
      id: "test-switch-1"
    })));
    expect(checkbox).toMatchSnapshot();
  });
  test('renders as checked', function () {
    var checkbox = create(renderWithTheme( /*#__PURE__*/React.createElement(Switch, {
      id: "test-switch-2",
      checked: true
    })));
    expect(checkbox).toMatchSnapshot();
  });
});
//# sourceMappingURL=switch.test.js.map