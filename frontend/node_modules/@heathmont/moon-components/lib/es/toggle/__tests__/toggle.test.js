import React from 'react';
import { create } from 'react-test-renderer';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';
import 'jest-styled-components';
import Toggle from '../Toggle';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

describe('Toggle', function () {
  test('renders correctly', function () {
    var toggle = create(renderWithTheme( /*#__PURE__*/React.createElement(Toggle, null)));
    expect(toggle).toMatchSnapshot();
  });
  test('positioned static when "topRight=false"', function () {
    var toggle = create(renderWithTheme( /*#__PURE__*/React.createElement(Toggle, {
      topRight: false
    })));
    expect(toggle).toMatchSnapshot();
  });
});
//# sourceMappingURL=toggle.test.js.map