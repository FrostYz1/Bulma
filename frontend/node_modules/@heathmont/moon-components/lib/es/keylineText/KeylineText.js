import styled from 'styled-components';
import { rem } from '@heathmont/moon-utils';
import Caption from '../caption/Caption';
var KeylineText = styled(Caption).withConfig({
  displayName: "KeylineText",
  componentId: "wzk90x-0"
})(function (_ref) {
  var _ref$theme = _ref.theme,
      borderWidth = _ref$theme.borderWidth,
      color = _ref$theme.color,
      space = _ref$theme.space;
  return {
    display: 'flex',
    width: '100%',
    alignItems: 'center',
    whiteSpace: 'nowrap',
    color: color.trunks[100],
    '&::before, &::after': {
      content: '""',
      display: 'inline-block',
      width: '100%',
      flex: '0 1 auto',
      height: borderWidth,
      backgroundColor: color.beerus[100]
    },
    '&::before': {
      marginRight: rem(space.small)
    },
    '&::after': {
      marginLeft: rem(space.small)
    }
  };
});
export default KeylineText;
//# sourceMappingURL=KeylineText.js.map