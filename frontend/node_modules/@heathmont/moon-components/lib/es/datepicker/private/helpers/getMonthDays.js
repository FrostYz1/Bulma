import lastDayOfMonth from 'date-fns/lastDayOfMonth';
import eachDayOfInterval from 'date-fns/eachDayOfInterval';
import startOfWeek from 'date-fns/startOfWeek';
import endOfWeek from 'date-fns/endOfWeek';
import startOfMonth from 'date-fns/startOfMonth';
export var getMonthDays = function getMonthDays(_ref) {
  var date = _ref.date,
      weekStartsOn = _ref.weekStartsOn;
  var firstDay = startOfMonth(date);
  var lastDay = lastDayOfMonth(date);
  return eachDayOfInterval({
    start: startOfWeek(firstDay, {
      weekStartsOn: weekStartsOn
    }),
    end: endOfWeek(lastDay, {
      weekStartsOn: weekStartsOn
    })
  });
};
//# sourceMappingURL=getMonthDays.js.map