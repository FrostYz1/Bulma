import startOfWeek from 'date-fns/startOfWeek';
import endOfWeek from 'date-fns/endOfWeek';
import eachDayOfInterval from 'date-fns/eachDayOfInterval';
import format from 'date-fns/format';
export var getWeekDayLabels = function getWeekDayLabels(_ref) {
  var _ref$weekStartsOn = _ref.weekStartsOn,
      weekStartsOn = _ref$weekStartsOn === void 0 ? 1 : _ref$weekStartsOn,
      locale = _ref.locale;
  return eachDayOfInterval({
    start: startOfWeek(new Date(), {
      weekStartsOn: weekStartsOn
    }),
    end: endOfWeek(new Date(), {
      weekStartsOn: weekStartsOn
    })
  }).map(function (day) {
    return format(day, 'EEEEE', {
      locale: locale
    });
  });
};
//# sourceMappingURL=getWeekDayLabels.js.map