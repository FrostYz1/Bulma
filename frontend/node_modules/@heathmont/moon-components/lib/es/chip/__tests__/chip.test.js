import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';
import IconProfile from '@heathmont/moon-assets/src/icons/IconProfile';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

import Chip from '../Chip';
describe('Chip', function () {
  test('renders correctly', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, null, "Chip")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly with active status', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, {
      active: true
    }, "Active")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly with small size', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, {
      size: "small"
    }, "Small")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly default size', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, null, "Medium")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly with Left Icon', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, {
      iconLeft: /*#__PURE__*/React.createElement(IconProfile, null)
    }, "Chip with left icon")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly with Right Icon', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, {
      iconRight: /*#__PURE__*/React.createElement(IconProfile, null)
    }, "Chip with right icon")));
    expect(chip).toMatchSnapshot();
  });
  test('renders correctly with Right and left Icons', function () {
    var chip = create(renderWithTheme( /*#__PURE__*/React.createElement(Chip, {
      iconLeft: /*#__PURE__*/React.createElement(IconProfile, null),
      iconRight: /*#__PURE__*/React.createElement(IconProfile, null)
    }, "Chip with icons on both sides")));
    expect(chip).toMatchSnapshot();
  });
});
//# sourceMappingURL=chip.test.js.map