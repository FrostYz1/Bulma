import React from 'react';
import { create } from 'react-test-renderer';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';
import 'jest-styled-components';
import FileInput from '../FileInput';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

describe('File Input', function () {
  test('renders a file input with default placeholder', function () {
    var fileInput = create(renderWithTheme( /*#__PURE__*/React.createElement(FileInput, {
      id: "fileInput1"
    })));
    expect(fileInput).toMatchSnapshot();
  });
  test('renders a file input with custom placeholder', function () {
    var fileInput = create(renderWithTheme( /*#__PURE__*/React.createElement(FileInput, {
      id: "fileInput2",
      placeholder: "Choose a photo\u2026"
    })));
    expect(fileInput).toMatchSnapshot();
  });
  test('renders a file input with custom label', function () {
    var fileInput = create(renderWithTheme( /*#__PURE__*/React.createElement(FileInput, {
      id: "fileInput3",
      label: "ID, passport, etc."
    })));
    expect(fileInput).toMatchSnapshot();
  });
  test('renders a file input with custom label and placeholder', function () {
    var fileInput = create(renderWithTheme( /*#__PURE__*/React.createElement(FileInput, {
      id: "fileInput4",
      label: "Upload photo",
      placeholder: "ID, passport, etc."
    })));
    expect(fileInput).toMatchSnapshot();
  });
  test('renders as error', function () {
    var fileInput = create(renderWithTheme( /*#__PURE__*/React.createElement(FileInput, {
      id: "fileInput5",
      error: true
    })));
    expect(fileInput).toMatchSnapshot();
  });
});
//# sourceMappingURL=file-input.test.js.map