import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

import Badge from '../../badge/Badge';
import CardBalance from '../CardBalance';
describe('CardBalance', function () {
  test('renders correctly', function () {
    var balance = create(renderWithTheme( /*#__PURE__*/React.createElement(CardBalance, {
      from: {
        currency: 'Bitcoin',
        value: '1526.56',
        unit: 'mBTC',
        unitTitle: 'Millibitcoin'
      }
    })));
    expect(balance).toMatchSnapshot();
  });
  test('renders with currency if no unitTitle provided', function () {
    var balance = create(renderWithTheme( /*#__PURE__*/React.createElement(CardBalance, {
      from: {
        currency: 'Bitcoin',
        value: '1526.56',
        unit: 'mBTC'
      }
    })));
    expect(balance).toMatchSnapshot();
  });
  test('renders correctly with a badge', function () {
    var balance = create(renderWithTheme( /*#__PURE__*/React.createElement(CardBalance, {
      badge: /*#__PURE__*/React.createElement(Badge, null, "Active"),
      from: {
        currency: 'Bitcoin',
        value: '1526.56',
        unit: 'mBTC',
        unitTitle: 'Millibitcoin'
      }
    })));
    expect(balance).toMatchSnapshot();
  });
});
//# sourceMappingURL=balance.test.js.map