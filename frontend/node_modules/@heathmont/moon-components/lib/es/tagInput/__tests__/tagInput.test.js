import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';
import { TagInputMain } from '../TagInput';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

describe('TagInput', function () {
  test('renders correctly', function () {
    var tabLink = create(renderWithTheme( /*#__PURE__*/React.createElement(TagInputMain, null)));
    expect(tabLink).toMatchSnapshot();
  });
  test('renders with focused state', function () {
    var tabLink = create(renderWithTheme( /*#__PURE__*/React.createElement(TagInputMain, {
      focused: true
    })));
    expect(tabLink).toMatchSnapshot();
  });
  test('renders with error state', function () {
    var tabLink = create(renderWithTheme( /*#__PURE__*/React.createElement(TagInputMain, {
      error: true
    })));
    expect(tabLink).toMatchSnapshot();
  });
});
//# sourceMappingURL=tagInput.test.js.map