/* eslint-disable no-console */
import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';
import NumberRange from '../NumberRange';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

describe('Number Range', function () {
  test('renders correctly', function () {
    var NumberRangeEl = create(renderWithTheme( /*#__PURE__*/React.createElement(NumberRange, {
      text: "Enter a minimum, maximum or range limit",
      lteLabel: "Max",
      gteLabel: "Min",
      value: {
        gte: 16,
        lte: 20
      },
      onChange: function onChange(value) {
        return console.log(value);
      },
      lteDisabled: false,
      lteError: false,
      gteDisabled: false,
      gteError: false,
      maxWidth: 246
    })));
    expect(NumberRangeEl).toMatchSnapshot();
  });
});
//# sourceMappingURL=numberRange.test.js.map