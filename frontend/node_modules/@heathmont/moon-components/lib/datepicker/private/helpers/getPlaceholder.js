"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPlaceholder = void 0;

var _isValid = _interopRequireDefault(require("date-fns/isValid"));

var _format = _interopRequireDefault(require("date-fns/format"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var DateFormatWithTime = 'dd/MM/yyyy HH:mm';
var DateFormat = 'dd/MM/yyyy';

var getPlaceholder = function getPlaceholder(_ref) {
  var startDate = _ref.startDate,
      endDate = _ref.endDate,
      range = _ref.range,
      config = _ref.config,
      translations = _ref.translations;

  if (!startDate && !endDate && !range) {
    return translations.placeholder;
  }

  if (range in translations) {
    return translations[range];
  }

  if ((0, _isValid["default"])(startDate) && (0, _isValid["default"])(endDate)) {
    var dateFormat = config.withHoursAndMinutes ? DateFormatWithTime : DateFormat;
    return "".concat((0, _format["default"])(startDate, dateFormat), " - ").concat((0, _format["default"])(endDate, dateFormat));
  }

  return translations.placeholder;
};

exports.getPlaceholder = getPlaceholder;
//# sourceMappingURL=getPlaceholder.js.map