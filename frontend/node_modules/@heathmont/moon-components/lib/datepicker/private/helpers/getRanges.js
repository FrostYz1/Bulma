"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getRanges = void 0;
var future = ['tomorrow', 'nextWeek', 'nextMonth'];
var past = ['lastMonth', 'lastWeek', 'yesterday', 'thisWeek', 'thisMonth'];

var getRanges = function getRanges(_ref) {
  var ranges = _ref.ranges,
      _ref$onlyFuture = _ref.onlyFuture,
      onlyFuture = _ref$onlyFuture === void 0 ? false : _ref$onlyFuture,
      _ref$without24AndToda = _ref.without24AndToday,
      without24AndToday = _ref$without24AndToda === void 0 ? false : _ref$without24AndToda;

  if (ranges) {
    return ranges;
  }

  if (onlyFuture) {
    return future;
  }

  if (!without24AndToday) {
    return [].concat(past, ['last24hours', 'today']);
  }

  return past;
};

exports.getRanges = getRanges;
//# sourceMappingURL=getRanges.js.map