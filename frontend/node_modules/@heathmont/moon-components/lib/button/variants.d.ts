import { CSSObject } from 'styled-components';
import { Theme } from '@heathmont/moon-themes';
declare type ButtonVariants = keyof ButtonVariantStyles;
declare type ButtonVariantStyles = {
    primary: CSSObject;
    secondary: CSSObject;
    tertiary: CSSObject;
    highlight: CSSObject;
    default: CSSObject;
};
/**
 * Styles that extend `buttonBase` to change the button's cosmetic appearance.
 * Accessed via `buttonVariant(key)(theme)`.
 *
 * For usage guidelines, see the README.
 */
declare const buttonVariant: (key: ButtonVariants) => ({ color }: Theme) => CSSObject;
/**
 * buttonShadow
 *
 * Removes user-agent `:focus` styles and generates a box-shadow using the
 * color provided.
 */
export declare const buttonShadow: (color: string) => ({ space, }: Theme) => CSSObject;
export type { ButtonVariants };
export { buttonVariant };
declare const _default: {
    buttonVariant: (key: "default" | "primary" | "secondary" | "tertiary" | "highlight") => ({ color }: Theme) => CSSObject;
};
export default _default;
//# sourceMappingURL=variants.d.ts.map