import React from 'react';
import { create } from 'react-test-renderer';
import 'jest-styled-components';
import { sportsbetDark, ThemeProvider } from '@heathmont/moon-themes';

var renderWithTheme = function renderWithTheme(component) {
  return /*#__PURE__*/React.createElement(ThemeProvider, {
    theme: sportsbetDark
  }, component);
};

import Badge from '../Badge';
describe('Badge', function () {
  test('renders correctly', function () {
    var badge = create(renderWithTheme( /*#__PURE__*/React.createElement(Badge, null, "Active")));
    expect(badge).toMatchSnapshot();
  });
  test('renders correctly with color', function () {
    var badge = create(renderWithTheme( /*#__PURE__*/React.createElement(Badge, {
      color: "red"
    }, "Red badge")));
    expect(badge).toMatchSnapshot();
  });
  test('renders correctly with background color', function () {
    var badge = create(renderWithTheme( /*#__PURE__*/React.createElement(Badge, {
      backgroundColor: "red"
    }, "Red background badge")));
    expect(badge).toMatchSnapshot();
  });
  test('renders correctly default size', function () {
    var badge = create(renderWithTheme( /*#__PURE__*/React.createElement(Badge, {
      backgroundColor: "red"
    }, "Default size badge")));
    expect(badge).toMatchSnapshot();
  });
  test('renders correctly small size', function () {
    var badge = create(renderWithTheme( /*#__PURE__*/React.createElement(Badge, {
      backgroundColor: "red",
      size: "small"
    }, "Default size badge")));
    expect(badge).toMatchSnapshot();
  });
});
//# sourceMappingURL=badge.test.js.map