import { renderToStaticMarkup } from 'react-dom/server';

var inlineSvg = function inlineSvg(svg) {
  var rendered = renderToStaticMarkup(svg);
  var encodedSVG = encodeURIComponent(rendered);
  return "url('data:image/svg+xml;utf8, ".concat(encodedSVG, "')");
};

export default inlineSvg;
//# sourceMappingURL=inlineSvg.js.map