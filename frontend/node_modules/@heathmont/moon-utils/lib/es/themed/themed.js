import has from 'lodash.has';
import get from 'lodash.get';
import rem from '../rem/rem';

var themed = function themed(key, value) {
  return function (theme) {
    var path = "".concat(String(key), ".").concat(value);

    if (has(theme, path)) {
      var themedValue = get(theme, path);
      return ['space', 'radius'].includes(String(key)) ? rem(themedValue) : themedValue;
    }

    return value;
  };
};

export default themed;
//# sourceMappingURL=themed.js.map